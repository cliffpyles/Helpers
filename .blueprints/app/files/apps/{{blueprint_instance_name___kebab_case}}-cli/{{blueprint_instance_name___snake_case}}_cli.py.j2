#!/usr/bin/env python3

import glob
import requests
from urllib.parse import urljoin
import os
import sys
import click
import yaml
from pathlib import Path
import shutil
from jinja2 import Environment, FileSystemLoader, Template, TemplateNotFound
from pipes import capitalize, upper, lower, kebab_case, snake_case, camel_case, pascal_case

# Helper functions
def load_config_file(file_path):
    with open(file_path) as f:
        return yaml.safe_load(f)


def load_config():
    config_path = Path.home() / ".{{blueprint_instance_name | kebab_case}}" / "config.yaml"
    if config_path.exists():
        return load_config_file(config_path)
    return {}



# CLI commands
@click.group()
def cli():
    pass


@cli.command(help="Initialize a project for local blueprint support.")
def init():
    config = load_config()
    click.echo("Command executed")


if __name__ == "__main__":
    cli()
